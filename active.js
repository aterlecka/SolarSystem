let scene,
    camera,
    light,
    renderer,
    earthObject;

let WIDTH = window.innerWidth - 30,
    HEIGHT = window.innerHeight - 30;

let angle = 45,
    aspect = WIDTH / HEIGHT,
    near = 0.1,
    far = 3000;
